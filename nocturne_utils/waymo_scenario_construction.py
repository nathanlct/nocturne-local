"""Construct a scenarios.json file from a waymos protobuf
"""


def load_protobuf(protobuf_path):
    protobuf = None
    return protobuf


def get_actions_from_protobuf(protobuf, veh_id, time_step):
    return None


def waymo_to_scenario(scenario_path, protobuf):
    pass
    # read the protobuf file to get the right state

    # write the json file
    # construct the road geometries
    # place the initial position of the vehicles
